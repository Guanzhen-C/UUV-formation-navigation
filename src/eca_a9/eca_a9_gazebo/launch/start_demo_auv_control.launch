<launch>
  <!-- Vehicle's initial position -->
  <arg name="x" default="1000"/>
  <arg name="y" default="0"/>
  <arg name="z" default="-1000"/>
  <arg name="yaw" default="1.57"/>

  <arg name="record" default="false"/>
  <arg name="bag_filename" default="recording.bag"/>

  <arg name="use_ned_frame" default="false"/>
  <!-- AUV speed control parameter -->
  <arg name="max_forward_speed" default="5.0"/>
  <!-- Mission latitude in radians for Earth Coriolis force calculation -->
  <arg name="mission_latitude" default="0.3183" doc="Mission latitude in radians (default: 18.25°N Sanya)"/>
  <!-- Expose path radius controls to the included controller launch -->
  <arg name="dubins_radius" default="1000"/>
  <arg name="idle_radius" default="1000"/>

  <include file="$(find eca_a9_description)/launch/upload_eca_a9.launch">
    <arg name="x" value="$(arg x)"/>
    <arg name="y" value="$(arg y)"/>
    <arg name="z" value="$(arg z)"/>
    <arg name="yaw" value="$(arg yaw)"/>
    <arg name="mission_latitude" value="$(arg mission_latitude)"/>
  </include>

  <include file="$(find eca_a9_control)/launch/start_geometric_tracking_control.launch">
    <arg name="dubins_radius" value="$(arg dubins_radius)"/>
    <arg name="idle_radius" value="$(arg idle_radius)"/>
    <arg name="max_forward_speed" value="$(arg max_forward_speed)"/>
  </include>
    
  <!-- Initialize the recording fo the simulation according to the record flag  -->
  <include file="$(find eca_a9_gazebo)/launch/record_demo.launch">
    <arg name="record" value="$(arg record)"/>
    <arg name="use_ned_frame" value="$(arg use_ned_frame)"/>
    <arg name="bag_filename" value="$(arg bag_filename)"/>
  </include>
</launch>
